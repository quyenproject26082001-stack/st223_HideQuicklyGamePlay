<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000">

    <com.wanted.poster.maker.activity_app.game.GameView
        android:id="@+id/gameView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Nút back -->
    <ImageButton
        android:id="@+id/btnBack"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="top|start"
        android:layout_margin="12dp"
        android:background="@drawable/bg_circle_black_50"
        android:src="@drawable/ic_back"
        android:contentDescription="Back" />

    <!-- Nút bật/tắt debug collision -->
    <Button
        android:id="@+id/btnDebug"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:layout_gravity="top|end"
        android:layout_margin="12dp"
        android:text="Debug OFF"
        android:textSize="12sp"
        android:paddingHorizontal="12dp"
        android:backgroundTint="#AA000000"
        android:textColor="#FFFFFF" />

    <!-- Test: vào màn Choose Number -->
    <Button
        android:id="@+id/btnPlayGame"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_margin="24dp"
        android:text="▶  Play Game"
        android:textSize="14sp"
        android:paddingHorizontal="20dp"
        android:backgroundTint="#CC2E7D32"
        android:textColor="#FFFFFF" />

</FrameLayout>
